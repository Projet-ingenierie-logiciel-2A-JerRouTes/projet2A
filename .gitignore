# ==============================================================================
# FICHIER .GITIGNORE - PROJET 2A (Gestionnaire de Stock)
# Regroupe les exclusions pour Python (Ruff/Pytest) et React (Node.js)
# ==============================================================================

# --- [ NODE.JS / REACT ] ---
# On ne commit JAMAIS les bibliothèques installées (trop lourd)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp/
.pnp.js

# Dossiers de build (fichiers générés pour la production)
dist/
build/
.next/
out/

# --- [ PYTHON / BACKEND ] ---
# Fichiers compilés et cache
__pycache__/
*.py[codz]
*$py.class
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Environnements virtuels (indispensable pour l'internat lundi)
.venv/
venv/
env/
ENV/
.env
.envrc

# Tests et Couverture
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml

# --- [ SYSTÈME ET ÉDITEURS ] ---
# Windows (Fichiers de prévisualisation)
Thumbs.db
ehthumbs.db
Desktop.ini

# VS Code (Paramètres locaux de l'éditeur)
.vscode/
# Note : On ignore souvent .vscode pour ne pas imposer ses réglages aux autres

# --- [ BASES DE DONNÉES ] ---
# Si tu utilises SQLite en local pour tes tests
*.sqlite3
*.sqlite3-journal
